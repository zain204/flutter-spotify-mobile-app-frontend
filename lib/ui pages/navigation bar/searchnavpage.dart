import 'package:flutter/material.dart';

class Searchnavpage extends StatelessWidget {
  const Searchnavpage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("search screen"),
      ),
    );
  }
}